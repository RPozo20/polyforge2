// types/next-auth.d.ts
// Extends NextAuth default types to include user.id in the session
import "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      name?: string | null;
      email?: string | null;
      image?: string | null;
    };
  }
}
